# VM-specific settings
#
# Keep the virtual display simple and avoid hardware-specific xrandr layouts.
exec --no-startup-id xset s off
exec --no-startup-id xset -dpms
exec --no-startup-id xset s noblank
