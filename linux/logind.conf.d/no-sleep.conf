[Login]
# Disable automatic suspend on idle.
# Without this, the system may suspend and fail to resume, which can corrupt
# the filesystem and break GRUB on desktop machines.
# Works on both laptop and desktop. Applies to any systemd-based distro.
IdleAction=ignore
