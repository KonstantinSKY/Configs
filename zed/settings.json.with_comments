{
  // ==========================================
  // ZED EDITOR CONFIGURATION
  // ==========================================
  // For complete documentation: https://zed.dev/docs/configuring-zed
  // To see all default settings: cmd-shift-p > "zed: open default settings"

  // "active_pane_modifiers": {
  //   "border_size": 10.0,
  //   "inactive_opacity": 1.0,
  //   "border_style": "solid",
  //   "border_color": "#002000",
  //   "border_radius": 5.0,
  //   "border_width": 2.0,
  //   "border_padding": 5.0,
  //   "border_margin": 5.0,
  //   "border_opacity": 0.5
  // },

  // ==========================================
  // AI ASSISTANT SETTINGS
  // ==========================================
  // "agent": {
  //   // Configuration for Zed's AI assistant
  //   "model_parameters": [], // Additional parameters to pass to the AI model
  //   "default_profile": "write", // Default profile to use for AI interactions (write, review, etc.)
  //   "version": "2" // AI agent version to use
  // },
  // "agent": {
  //   "model_parameters": [],
  //   "default_profile": "ask",
  //   "version": "2"
  // },

  "agent": {
    "model_parameters": [],
    "default_profile": "write",
    "version": "2"
  },
  "language_models": {
    "n8n-local": {
      "provider": "openai",
      "model": "n8n-local",
      "api_url": "http://localhost:5678/webhook-test",
      "api_key": "not-needed"
    }
  },
  "chat": {
    "default_model": "n8n-local"
  },
  // "context_servers": {
  //   // Configuration for context servers used by AI
  //   "mcp-server-context7": {
  //     // Context7 server configuration
  //     "settings": {
  //       // Settings for this context server
  //       "default_minimum_tokens": "10000" // Minimum tokens to use for context by default
  //     }
  //   }
  // },
  // "agent": {
  //   "default_profile": "write",
  //   "default_model": {
  //     "provider": "zed.dev",
  //     "model": "claude-sonnet-4"
  //   },
  //   "model_parameters": [],
  //   "always_allow_tool_actions": true,
  //   "version": "2"
  // },

  // ==========================================
  // EDITOR BEHAVIOR
  // ==========================================
  "autosave": "on_focus_change", // When to automatically save files (on_focus_change, on_window_change, off)
  "vim_mode": true, // Enable Vim keybindings and modal editing
  "format_on_save": "on", // Automatically format files when saving (on, off, language_server)
  "remove_trailing_whitespace_on_save": true, // Remove trailing whitespace when saving files
  "ensure_final_newline_on_save": true, // Ensure files end with a newline character when saving
  "soft_wrap": "editor_width", // How to wrap long lines (none, editor_width, preferred_line_length)
  "show_whitespaces": "selection", // When to show whitespace characters (all, selection, none)
  "show_wrap_guides": true, // Show vertical lines at wrap guide positions
  "wrap_guides": [80, 120], // Column positions where to show wrap guides
  "preferred_line_length": 100, // Preferred line length for formatting and wrapping

  // ==========================================
  // APPEARANCE & FONTS
  // ==========================================
  "ui_font_size": 16, // Font size for UI elements (panels, tabs, etc.)
  "buffer_font_size": 14, // Font size for text in editor buffers
  "theme": {
    // Theme configuration
    "mode": "dark", // Theme mode (light, dark, system)
    "light": "Gruvbox Light", // Theme to use in light mode
    "dark": "One Dark" // Theme to use in dark mode
  },
  "icon_theme": {
    // Icon theme configuration
    "mode": "dark", // Icon theme mode (light, dark, system)
    "light": "Material Icon Theme", // Icon theme to use in light mode
    "dark": "Material Icon Theme" // Icon theme to use in dark mode
  },

  // ==========================================
  // CUSTOM SYNTAX HIGHLIGHTING COLORS
  // ==========================================
  "experimental.theme_overrides": {
    // Custom overrides for the current theme
    "syntax": {
      // Syntax highlighting color customizations
      "keyword": {
        // Styling for language keywords (if, else, for, etc.)
        "color": "#ff6b6b", // Red color for keywords
        "font_weight": "bold" // Make keywords bold
      },
      "function": {
        // Styling for function names
        "color": "#4ecdc4" // Teal color for functions
      },
      "string": {
        // Styling for string literals
        "color": "#95e1d3", // Light green color for strings
        "font_style": "italic" // Make strings italic
      },
      "comment": {
        // Styling for code comments
        "color": "#6c757d", // Gray color for comments
        "font_style": "italic" // Make comments italic
      },
      "type": {
        // Styling for type names
        "color": "#ffd93d" // Yellow color for types
      },
      "variable": {
        // Styling for variable names
        "color": "#74b9ff" // Blue color for variables
      },
      "number": {
        // Styling for numeric literals
        "color": "#fd79a8" // Pink color for numbers
      },
      "attribute": {
        // Styling for attributes/annotations
        "color": "#fdcb6e" // Orange color for attributes
      }
    }
  },

  // ==========================================
  // TERMINAL CONFIGURATION
  // ==========================================
  "terminal": {
    // Integrated terminal settings
    "shell": {
      // Shell configuration
      "program": "/bin/zsh" // Path to the shell program to use
    },
    "working_directory": "current_project_directory", // Default working directory for new terminals
    "blinking": "on" // Whether the terminal cursor should blink
  },

  // ==========================================
  // SPELL CHECK SETTINGS
  // ==========================================
  "spell_check": {
    // Spell checking configuration
    "enabled": true, // Enable spell checking
    "languages": ["en-US", "ru-RU"] // Language to use for spell checking
  },

  // ==========================================
  // GIT INTEGRATION
  // ==========================================
  "git": {
    // Git version control settings
    "git_binary_path": "/usr/bin/git", // Path to the git binary
    "inline_blame": {
      // Git blame annotation settings
      "enabled": true // Show git blame information inline
    },
    "gutter_debounce": 1000 // Delay in milliseconds before updating git gutter indicators
    // "auto_fetch": true, // Automatically fetch from remote repositories
    // "auto_fetch_interval": 300 // Interval in seconds between automatic fetches
  },

  // ==========================================
  // GITHUB INTEGRATION
  // ==========================================
  //   "github": { // GitHub integration settings
  //     "online_
  // status": true // Show online status in GitHub integration
  //   },

  // ==========================================
  // LANGUAGE-SPECIFIC SETTINGS
  // ==========================================
  "languages": {
    // Per-language configuration overrides
    "Rust": {
      // Settings specific to Rust files
      "tab_size": 4, // Number of spaces for indentation
      "hard_tabs": false, // Use spaces instead of tab characters
      "formatter": "language_server", // Use language server for formatting
      "format_on_save": "on", // Format Rust files when saving
      "show_edit_predictions": true // Show AI-powered edit predictions
    },
    "JavaScript": {
      // Settings specific to JavaScript files
      "tab_size": 2, // Use 2 spaces for indentation
      "formatter": "prettier" // Use Prettier for formatting
    },
    "TypeScript": {
      // Settings specific to TypeScript files
      "tab_size": 2, // Use 2 spaces for indentation
      "formatter": "prettier" // Use Prettier for formatting
    },
    "JSON": {
      // Settings specific to JSON files
      "tab_size": 2, // Use 2 spaces for indentation
      "formatter": "prettier" // Use Prettier for formatting
    },
    "TOML": {
      // Settings specific to TOML files
      "tab_size": 4, // Use 4 spaces for indentation
      "hard_tabs": false // Use spaces instead of tab characters
    },
    "Markdown": {
      // Settings specific to Markdown files
      "soft_wrap": "editor_width", // Wrap lines at editor width
      "spell_check": {
        "enabled": true,
        "languages": ["en-US", "ru-RU"]
      },
      "preferred_line_length": 80 // Prefer 80 character line length
    }
  },

  // ==========================================
  // CODE COMPLETIONS & LSP
  // ==========================================
  "show_completions_on_input": true, // Show code completions as you type
  "show_completion_documentation": true, // Show documentation for completion items
  "completion_documentation_secondary_query_debounce": 300, // Delay before showing secondary documentation
  "inlay_hints": {
    // Language server inlay hints configuration
    "enabled": true, // Enable inlay hints
    "show_type_hints": true, // Show type information hints
    "show_parameter_hints": true // Show parameter name hints
  },

  // ==========================================
  // FILE MANAGEMENT
  // ==========================================
  "file_scan_exclusions": [
    // Directories and files to exclude from file scanning
    "**/.git", // Exclude git directories
    "**/target", // Exclude Rust target directories
    "**/node_modules", // Exclude Node.js modules
    "**/.next", // Exclude Next.js build directories
    "**/dist", // Exclude distribution directories
    "**/build", // Exclude build directories
    "**/*.log" // Exclude log files
  ],

  // ==========================================
  // WORKSPACE & SESSION
  // ==========================================
  "restore_on_startup": "last_workspace", // What to restore on startup (last_workspace, none)

  // ==========================================
  // PRIVACY SETTINGS
  // ==========================================
  "telemetry": {
    // Telemetry and data collection settings
    "metrics": false, // Don't send usage metrics
    "diagnostics": false // Don't send diagnostic information
  }

  // ==========================================
  // PERFORMANCE & UI
  // ==========================================
}
